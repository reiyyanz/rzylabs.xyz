function Contact() {
  return (
    <section id="contact">
      <div className="section-head">
        <div className="section-title">contact / signals</div>
        <div className="pill accent">stay in the lab loop</div>
      </div>
      <div className="tile">
        <h3>get updates</h3>
        <p className="subtext">
          upcoming: rzylabs noir drops, new archive sets, finance dashboards with secure data. opt-in for release signals.
        </p>
        <div className="cta">
          <button className="primary">join the list</button>
          <button>email</button>
        </div>
      </div>
    </section>
  );
}

export default Contact;
